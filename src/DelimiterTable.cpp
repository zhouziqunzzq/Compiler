#include<bits/stdc++.h>
#include "DelimiterTable.h"

using namespace std;

DelimiterTable::DelimiterTable() {
    entry("+");
    entry("-");
    entry("*");
    entry("/");
    entry("(");
    entry(")");
    entry(",");
    entry(";");
    entry("=");
}
